# Verification and evidence contract

## Deterministic fixtures and provenance

Primary verification is credential-free and networkless after `npm ci --ignore-scripts`. Frozen source and golden ownership, byte counts, hashes, and allowed derivation are in [`test/oracles/manifest.json`](../test/oracles/manifest.json); `npm run verify:g001` verifies them. Before excluding the pinned Lua tree from product hardcoding checks, that command also proves its exact path inventory, byte counts, and SHA-256 values against [`native/lua-source-manifest.json`](../native/lua-source-manifest.json). Runtime output never creates its own expected golden.

The current verification slices perform no remote operation beyond lockfile-pinned, credential-free, read-only HTTPS package-registry/CDN `GET` and metadata `HEAD` during dependency bootstrap. Product CDN reads remain deferred, and product CDN deployment, publication, upload, mutation, deletion, purge, invalidation, configuration change, and FTP/SFTP access are prohibited. Non-CDN remote work, including FTP/SFTP, deployment, publication, and communication dependencies, is not globally banned; an active feature must define its scope, credentials, safety rules, and deterministic local test boundary first.

## Verification tiers

- `npm run verify:fast`: targeted format/docs/policy/type/unit checks; no device, UI, screenshot, broad E2E, or milestone claim. Budget: 120 seconds warm local and 5 minutes cold CI.
- `npm run verify:story -- <goal-id>`: the only story acceptance aggregator. It runs every activated story-owned focused check exactly once and lists them. Budget: 10 minutes, or 20 minutes for a declared native compilation story.
- `npm run verify:milestone`: one full active regression and cross-platform/UI/package/provenance matrix. Budget: 45 minutes excluding declared toolchain provisioning. It exits nonzero while any required layer is `DEFERRED(<owning-goal>)`.
- `npm run verify:ci`: the clean-CI entry; it delegates to `verify:milestone` exactly once. Never run both for one acceptance attempt.

Focused `verify:format`, `verify:docs`, `verify:policy`, `verify:type`, `verify:unit`, `verify:fixtures`, `verify:native`, `verify:runtime`, `verify:ui`, and `verify:provenance` commands are diagnostic reruns, not a second acceptance sequence. Package diagnostics report their manifest deferment until activated. [`verification/manifest.json`](../verification/manifest.json) owns activation, command, inputs, outputs, risk, budgets, and story composition.

The active G002 native check is networkless. It proves the pinned archive and zero-diff source inventory, shared host harness, generated manifest-bound resources/runtime fixture, both the default-production and explicitly flagged CocoaPods/CMake source graphs, iOS simulator compilation, Android NDK compilation, the flag-gated Gradle source set, autolinking, sandbox, limits, symbols, and one adapter golden. The default graphs must exclude every G002 module/adapter source; `EXPO_PUBLIC_G002_NATIVE_HARNESS=1` must add the complete verification graph. Its local Development Build runner passes that flag to CocoaPods and Gradle, compiles the generated iOS workspace, inspects every packaged Mach-O for exactly one Lua provider and no second Lua dependency, and executes three JS-to-native create/evaluate/destroy cycles against the shared golden. The runner module exports only that real execution entrypoint; its result-shape validator remains private so callers cannot submit synthetic PASS evidence. The explicit verification flag is the only entry to this debug harness; ordinary application startup is unchanged.

The Development Build runner requires the exact installed React Native and Hermes versions in existing CocoaPods caches, derives temporary local Hermes and React Native dependency tarballs from those caches, builds remaining React Native and Expo code from installed sources, and executes CocoaPods beneath a macOS `deny network*` sandbox with `--no-repo-update`. It may invoke only a matching Gradle distribution already present in the local wrapper cache, and still passes Gradle `--offline`; it never starts the wrapper when that distribution is absent. Missing local Pod or pinned Gradle/plugin artifacts fail or report `BLOCKED` with `OFFLINE_DEPENDENCY_UNAVAILABLE`, without claiming an Android integration build or package. If the Android debug APK builds and passes package inspection but `adb` has no runtime target, the narrower Android adapter-runtime criterion reports `BLOCKED`. Offline NDK compilation alone is never Android Expo adapter execution.

The runner reserves an unused loopback port, injects that same port as `RCT_METRO_PORT` for CocoaPods/Xcode and `reactNativeDevServerPort` for Gradle, and starts only its own Metro process group there; it never reuses or terminates an existing service. Android execution creates an `adb reverse` only after refusing an existing same-port rule and removes only that created rule. The runner also refuses to replace pre-existing simulator/device apps, uninstalls only apps it installed, restores a simulator it booted to shutdown, and removes generated `ios/` and `android/` trees. These temporary debug package inspections are G002 evidence, not activation of the deferred production packaging layer or a product JS-engine decision.

The active G003 runtime check is networkless and non-UI. It validates the exact Host ledger and closed discriminated result/snapshot schema, validates every envelope emitted by shared-core hostile conformance, then covers revisions, immutable full snapshots, ordered one-shot commands, atomic commit/rollback, invalidation, every stated limit, resource loading, close/destroy races, request-token choreography, and two-runtime isolation. The final focused phase executes both platform C adapters against one checked-in shared expected golden, executes the production Objective-C++ adapter, executes the unchanged Swift Expo module's registered `create`/`dispatch`/`destroy` bodies through that production adapter and captures its real event path through a verification-only Expo protocol, compiles and executes the unchanged Kotlin Expo module through its real JNI boundary with a verification-only Android/Expo capture surface, and compiles the production iOS/Android targets. The ordinary Android Kotlin source set is also proven free of G002 while the flag-gated verification source is compiled separately. Missing module-path execution, adapter compilation, or parity cannot become PASS.

G003 may repeat only four named narrow G002 smokes inside that focused build: module `create/evaluate/destroy`, `_VERSION` plus sandbox/resource behavior, one global/`Form`/`DATAMANAGER`/control callback boundary, and the minimal adapter parity fixture. It never invokes `verify:native`, the G002 story aggregator, archive/license/source-inventory adoption, full G001 traces, UI, milestone, or CI. The canonical `npm run verify:story -- G003-implement-bounded-native-runtime` remains the sole G003 acceptance command and is run once only after independent pre-story review.

The active G004 UI check has exactly five targeted phase names: `parser-model`, `projection-render`, `runtime-client`, `unseen-generality`, and `module-stub-smoke`. A phase run is local and deterministic and never starts a Development Build, story, milestone, remote operation, G005, or G006 surface. The unphased manifest-owned command emits exactly eight story phases once and in order: `contract-registry`, the five targeted phases, `development-build`, and `policy-cleanup`. Generated-asset/App composition is part of `contract-registry`; there is no extra phase. Invocation counts are derived from the observed UI and Development-Build process records. Original, frozen synthetic, and post-freeze temporary unseen XMF bytes all enter the same root [`src/xmf.ts`](../src/xmf.ts) parser/projection API; the unseen input changes identities, order, layout, and transaction IDs without production registration or diff. `policy-cleanup` rejects legacy behavior identities while allowing the runtime contract's required internal `Form_OnFormClose` lifecycle event. XMS, `CtlImage`, malformed grammar, undeclared policy, and partial models fail closed.

The G004 module smoke compiles the unchanged Swift and Kotlin production module bodies through verification-only stubs and their real adapter/JNI paths; it does not invoke `verify:runtime` or claim a second G003 acceptance. It also compiles and evaluates the ordinary JavaScript package entry against a stub that records exactly one `AllNewMTSRuntime` request and no `AllNewMTSLua` request, while static checks require identical default/named runtime values and the named runtime type exports. App composition checks prove the fixed generated XMF bytes flow through the parser/renderer and the actual exported runtime value reaches `createRuntimeClient(runtime)`, with one exact create configuration and no Development Build dispatch. The generic `Success` client proof is independent of edit/button event-suffix serialization and executes no transaction completion/error/close, CCS/QRY, HS1200P08 Lua, G005 golden, login/auth, endpoint/credential, live transport, vendor SDK wrapper, or Native/MVigs analysis.

Before the sole story run, `node scripts/run-g004-development-build.mjs --preflight` performs read-only tool/cache/simulator/root-directory checks and records bounded Xcode, Swift, CocoaPods, JBR, Android SDK, and NDK provenance. Its Pod check derives the installed React Native `0.86.0` and Hermes V1 `250829098.0.14` versions, requires exactly one matching CocoaPods `Specs/External` entry for each of `ReactNativeDependencies` and `hermes-engine`, follows that entry's key into `External`, and requires nonempty plist, simulator binary, and representative header files from both cached payloads; directory presence alone is not sufficient. After releasing its port reservation, preflight returns `mutatedFiles: false` only if the git dirty path/status set, root native-directory state, G004 temp entries, and exact spec/content hashes still match its before snapshot. The build removes inherited Hermes-source, RN/RNCore test-version, and JS-engine selector overrides, disables RN dependency, RNCore, and Expo precompiled remote probes, explicitly selects Hermes V1, creates runner-owned local tarballs from the two exact caches before Expo prebuild, and points CocoaPods at those artifacts and an isolated temporary cache. A minimal temporary podspec runs through `pod ipc spec` beneath `deny network*` and proves the real upstream selectors choose both local `file://` artifacts, the consolidated dependency branch, source-built RNCore, and Hermes rather than third-party JSC without running `pod install`. The consolidated React Native dependency xcframework bypasses the source-built external fetches while `deny-external.sb` and `--no-repo-update` remain unchanged. Before build mutation, the unphased runner makes at most three total whole port-selection attempts (the initial attempt plus at most two retries): it records a kernel-selected non-8081 numeric identity as `runner-selected`, releases that selector for a strict sequential five-second/reaped SBPL truth suite, and reacquires the exact loopback port as `runner-guarded`; only an exact-port reacquisition collision may restart the whole selection and truth suite. `deny-external.sb` retains `(deny network*)`; `metro-owned-port.sb` denies external TCP, UDP, and wrong ports while same-port loopback, wildcard, and each available active nonloopback IPv4 bind are executable truth cases, with the active-address child also accepting and gracefully closing a parent connection. Those permitted wildcard/nonloopback cases document that SBPL does not enforce interface scope and never authorize Metro to listen there.

The user-authorized attempt-12 dependency bootstrap is separate from preflight and Story execution. It may perform one credential-free HTTPS `GET` from Gradle's official `https://services.gradle.org/distributions/gradle-8.13-bin.zip` endpoint through only the HTTPS `github.com/gradle/gradle-distributions` release and `release-assets.githubusercontent.com` asset redirects, with no other host, credential, product endpoint, or persisted signed redirect URL. The complete ZIP must match Gradle's published SHA-256 `20f1b1176237254a6fc204d8434196fa11a4cfb387567519c61556e8710aed78` before extraction and atomic adoption into wrapper-cache key `~/.gradle/wrapper/dists/gradle-8.13-bin/5xuhj0ry160q40clulazy9h7d`; a pre-existing target is never replaced. The adopted cache retains the verified ZIP, an empty `.ok` marker, and exactly one `gradle-8.13` tree.

The same bootstrap may fetch only Google's official Mac ARM command-line-tools archive `https://dl.google.com/android/repository/commandlinetools-mac_arm64-15859902_latest.zip`, whose published SHA-256 is `835b62a26162b229b441d1f6d4680383815a270809eb33522c0d480fa5002c4e`, verify it before extraction, and atomically adopt it as `~/Library/Android/sdk/cmdline-tools/15859902` without replacing a pre-existing target. That verified tool may use only credential-free HTTPS metadata and archive `GET`s below `https://dl.google.com/android/repository/` to install the two exact stable package identifiers `ndk;27.1.12297006` and `platforms;android-36` beneath the existing SDK root; it may update SDK package bookkeeping required by those two installs but may not update or remove any other package. It must rely solely on the already-present `android-sdk-license` acceptance hash `24333f8a63b6825ea9c5514f83c2829b004d1fee`: the bootstrap never invokes `--licenses`, supplies an acceptance answer, or proceeds if another license is requested. Preflight, focused phases, fast verification, and the Story remain networkless and only inspect/use the resulting local caches.

If the exact Apple dependency cache entries are absent, the same bootstrap may perform credential-free HTTPS `GET`s only for each archive and its `.sha256` sidecar at the two installed-React-Native-derived Maven Central URLs `https://repo1.maven.org/maven2/com/facebook/hermes/hermes-ios/250829098.0.14/hermes-ios-250829098.0.14-hermes-ios-debug.tar.gz` and `https://repo1.maven.org/maven2/com/facebook/react/react-native-artifacts/0.86.0/react-native-artifacts-0.86.0-reactnative-dependencies-debug.tar.gz`. The complete archives must respectively match the sidecar SHA-256 values `2f60e2db9b7e37c11a45ff5a9f3a14c5e585c444c1b055f2d96bebc1e3d94cc4` and `f6533c53527e75349346d07a2bba1a5cc1da4be8c41f93635a593047700b78f2` before extraction. A temporary `integrate_targets => false` CocoaPods project may then use only those local verified archives and the installed, integrity-owned podspecs beneath a `deny network*` sandbox to add exactly one `hermes-engine` `250829098.0.14` and one `ReactNativeDependencies` `0.86.0` external-cache entry. It must not update a spec repository, run against the product project, replace an existing exact entry, or delete unrelated older cache entries; staging and the temporary project are removed after exact required-file validation. The Story continues to derive its own local tarballs from those caches and remains networkless.

The final `node:net` loopback guard remains held through prebuild, Pods, generated settings checks, and Xcode build. It tracks accepted sockets, stops new accepts, destroys owned connections, and fails closed if release exceeds its bound, so request-like local bytes cannot pin handoff or cleanup. All detached Expo arguments and file descriptors are prepared before the guard release, and spawn is the immediately following operation with no fallback or reselection. `Metro-owned` begins only after `lsof -nP` proves exactly one `127.0.0.1:<runner-selected>` listener whose PID belongs to the recorded live launcher PGID. Readiness, immediate-prelaunch, and final checks repeat that ownership proof, require loopback success, and require failure through every then-active nonloopback IPv4 address. React Native 0.86 routes JavaScript `console` output through `com.facebook.react.log/javascript` unified logging, so the runner starts one exact-marker-filtered, owned simulator `log stream` before launch and accepts the value-redacted readiness marker from that file or the runner-owned `simctl --stdout/--stderr` files. It records honest 100 ms PID-scoped `lsof` samples. Evidence identifies the bounded unowned truth-probe and spawn handoffs and claims neither uninterrupted exclusive ownership, SBPL interface enforcement, nor continuous kernel-level denial between samples or for other simulator PIDs. Partial acquisition and later failures close guards and descriptors, prove the owned App PID exited, then prove both its App bundle container and exact LaunchServices `listapps` key remain absent for four consecutive 250 ms-spaced samples within 30 seconds after an idempotent uninstall. Only exit status `1` with the exact canonical `No such file or directory` diagnostic and empty stdout from `get_app_container` counts as container absence; mixed or otherwise unexpected status/diagnostic output fails closed; command-start, transport, invalid-result, command, plist-conversion, and malformed-list failures fail closed. The same dual-signal stability proof runs after one runner-owned simulator reboot before a bounded transition back to shutdown; simulator state probes use the supported `simctl list devices --json` form and filter the exact owned UDID, while each boot/bootstatus and shutdown/state-wait transition shares one command deadline; a stale owned registration may receive one further uninstall, whose exit status is secondary to the final stable state and whose launch error still fails. Cleanup also terminates and reaps probes, the unified-log stream, and the recorded Metro PGID, proves the port reusable with no listener and no active probe, preserves the primary failure, aggregates cleanup-only failures, and removes generated `ios/`, DerivedData, Pods, profiles, logs, socket evidence, and temporary files. The runner never reboots a simulator it did not boot. It refuses a pre-existing app, does not build Android, dispatch UI events, inspect pixels/accessibility, package/archive, access Native/MVigs material or the product CDN, or claim G005/G006.

Generated Metro evidence reads only physical records from `ios/Pods/Target Support Files/React-Core/*.xcconfig`, splits each at the first delimiter, and accepts only the exact LHS `GCC_PREPROCESSOR_DEFINITIONS`. At least one record is required, and every record must contain exactly one ASCII-whitespace-delimited raw token byte-equal to the upstream `RCT_METRO_PORT=${RCT_METRO_PORT}`; identical Debug/Release repetition is allowed, while quoted or malformed tokens, PBX text, generic normalization, and per-record duplicates fail. Numeric closure is separate: App output must contain exactly one byte-exact `Build settings from command line:` heading and one `Build settings for action build and target AllNewMTS:` heading, with exactly one selected decimal exact-LHS `RCT_METRO_PORT` record in each and no unclassified match; every other unindented nonempty heading resets classification, only the target-section record proves resolution, and bounded `{section,line,raw,rhs}` evidence survives success and cleanup/rethrow failure. React-Core resolved preprocessor records contain exactly one numeric Metro token overall, compiled-build argv contains exactly one selected assignment, and Expo argv contains exactly one `--localhost` plus one adjacent `--port <runner-selected>` pair and no `--offline`; offline mode is supplied only by `EXPO_OFFLINE=1` because Expo rejects simultaneous offline and host-selection flags. The runner pins `NODE_OPTIONS=--dns-result-order=ipv4first` so Expo's `localhost` server binds the already guarded and validated `127.0.0.1` endpoint instead of macOS preferring unguarded `::1`. Unrelated numbers are ignored. `node scripts/run-g004-development-build.mjs --metro-evidence-regression` runs the local positive/negative fixtures without prebuild, simulator, story, or remote work.

Only a failed compiled `xcodebuild` receives bounded diagnostic preservation. Stdout/stderr remain separate byte buffers; original byte counts are published, fatal UTF-8 decoding and finite named-value/Authorization/cookie/sensitive-URL redaction run over each complete stream before selection, and only sanitized counts/SHA-256 values exist. Each stream retains nonoverlapping UTF-8-safe 32,768-byte head/tail windows plus ordered first-match causal classes for exact build failure, failed-command heading, command failure, and raw `error:` lines with merged two-before/four-after context. Canonical stable-key JSON/base64 evidence uses fixed non-borrowing 65,536-byte earliest/latest causal partitions per stream, explicit retained/omitted match/context counts, deterministic 4,096-byte reduction, and a 524,288-byte total cap. The deep-frozen evidence/hash attached before cleanup is nested unchanged in the single post-cleanup `ALLNEWMTS_G004_BUILD_FAILURE=` envelope; cleanup count remains outside and Xcode status/signal remains primary. Formatter failure emits only `BUILD_FAILURE_EVIDENCE_FORMAT_ERROR` metadata with original counts or null, never raw output or an original digest.

On failure of the exact Development-Build child command, `scripts/verify-ui.mjs` removes every candidate marker line from ordinary diagnostics and publishes no raw child stdout/stderr. It forwards exactly one marker only when its suffix is byte-canonical stable-key JSON with the exact producer schema and keys, nonnegative safe cleanup count, lowercase matching evidence SHA-256, at most 524,288 evidence bytes, and at most 524,512 envelope bytes. Missing, duplicate, malformed, noncanonical, oversized, or hash-mismatched markers become one value-free `allnewmts.g004.build-failure-forward.v1` marker bounded to 1,024 bytes; the child failure remains primary. Other verifier command failures retain the existing 20,000-character tail. The private `--build-failure-forwarding-regression` path runs no build or parser phase and proves a canonical marker larger than 65,536 bytes crosses the story wrapper's inherited stdio intact while trailing diagnostics and planted secrets do not.

The producer writes the complete canonical failure-marker line synchronously to stdout in at most 16,384-byte chunks before rethrowing the same Xcode primary; positive progress is required and transient `EAGAIN` waits one millisecond before retry. Any other writer error is appended once after existing cleanup errors in `primaryError.cleanupErrors` and never replaces or contaminates the primary evidence/envelope. The private `--build-failure-marker-transport-child` uses the real formatter, default emitter, immediate same-primary throw, and more than 20,000 trailing stderr bytes without build, simulator, Metro, network, or repository mutation; the verifier regression proves byte/hash identity across runner, verifier, and inherited stdio and separately proves non-`EAGAIN` precedence.

The G011 nested-SwiftPM repair leaves the installed `expo-modules-jsi` package and scripts byte-identical and uses two explicit domains. Existing G004 owns its ordinary ambient operational temp, repository `ios/Pods`, sandbox profiles, CocoaPods artifacts/cache, Metro logs, and app DerivedData through the prior path-based cleanup contract. G011 B1 owns only one physical-repository `.omx/tmp/allnewmts-g011-*` root, package backup/recovery, and nested-SwiftPM staging. A pinned `/usr/bin/python3` verifies embedded custodian bytes and executes them with `compile`/`exec` in the same interpreter, never reopening helper source by pathname. That persistent custodian alone retains repository, `.omx`, `tmp`, journal, package, runner, and immutable committed-mirror recovery descriptors and performs complete restoration plus final acceptance itself. Bounded disposable copy/verification workers receive only package and staging duplicates, inventory/mutation/drift workers only a package duplicate, promotion only package plus the exact held shim descendant and shim executable, and regression only its exact held fixture descendant; no worker inherits the journal, mirror, runner root, backup root, or recovery anchors. Every worker bound is strictly shorter than its Node transport bound. A transport failure rejects every waiter, closes stdin exactly once, and lets EOF drive live-custodian recovery; a logical post-arm failure restores before its error response and leaves explicit cleanup usable. Node never signals the custodian before terminal cleanup and bounds TERM/KILL/reap only for workers or a post-terminal exit hang.

Before Expo prebuild or Pods can mutate the package, the custodian records exact canonical streams for `.DerivedData`, `.build`, `.swiftpm`, `.generated`, `Products/ExpoModulesJSI.xcframework`, and the whole package; independently verifies a source-equivalent full mirror with original modes; repeatedly checks the staging name/fd/type/mode and one shared deadline around copy, verification, and atomic adoption; fsyncs `BASELINE_COMMITTED`; then fsyncs `MUTATION_ARMED` with phase `prebuild`. Copy, verification, or readiness failure emits the same exact seven-key `allnewmts.g011.cleanup-complete.v1` terminal object as normal completion, with one `ANCHORED` record, only after removing staging, journal, backup, runner, invocation-created empty anchors, and closing all recovery fds. The exact stable-key journal chain is `ANCHORED`, `BASELINE_COMMITTED`, `MUTATION_ARMED`, `RESTORING`, `RESTORED`: each transition writes all bytes, fsyncs, immediately rewinds and reads back the complete file, then checks exact count, schema, keys, types, invariant identities and aggregates, null legality, phase/reason grammar, canonical LF bytes, and the SHA-256 chain. A failed restoration retry reuses the persisted `RESTORING` reason and sequence rather than appending a duplicate. Live-custodian full-mirror restoration mechanically repairs expected-root and outside-root modifications, deletions, and creations before exact five-root/whole acceptance. Its production coordinator attempts roots in unsigned UTF-8 path order, records actual injected `OSError` observations, deduplicates `(phase,path,error-code)`, preserves the original primary, continues later independent entries, and permits only a separate unhooked exact restore to finish the hostile fixture. `CLEANUP_COMPLETE` is the exact seven-key object and is emitted only after validated restoration state, runner/created-anchor removal, and closure of runner, tmp, `.omx`, and repository recovery fds; TERM/KILL diagnostics remain separate from it. G011-owned directory creation and writes retain every opened ancestor through verification; writes recheck that chain before and after the operation, bind the final pathname to the written fd, and require exact bytes plus EOF, mode, size, and single-link identity. Artifact recording performs the same final stat/open/fstat/read/post-stat binding, so a same-byte replacement cannot become accepted evidence. Each of the thirteen public hostile cases creates, uses, fully inventories, and removes its own distinct exact regression descendant, with no state inherited from another case, and compares exact pre/post case-root B1 bytes/hash, whole bytes/hash, outside identities/content, and residue entries. The absent-root case performs both success and original-primary all-five mutation/restoration; its raised sentinel traverses the same production primary/cleanup aggregator, remains the identical caught object, and carries the independently observed restore error before a separate unhooked exact restore. The drift case injects both mode and same-size byte changes through the shared production source-inventory-to-baseline-worker boundary and the production restore-to-final-verification boundary; every raised sentinel follows that same identity-preserving aggregator, attaches the actual mismatch object, then separately restores exact bytes. Present-root fixtures include an exact zero-byte file in every root. Escaping-link fixtures cover both a root path component and a nested escaping target, while ancestor substitution executes against inventory, deletion, and the shared exact-restore copy path. Success and precommit-failure fixtures compare recursively complete `.omx` and `.omx/tmp` entry inventories plus exact anchor type, mode, device, and inode and prove the hostile `/var` `TMPDIR` is never selected; when `.omx/tmp` is initially absent, the oracle records `present:false` with null identity and an empty inventory and requires cleanup to restore that absence. The focused unit wrapper allows this bounded regression 90 seconds inside the 120-second unit and warm-fast budgets. Executable fixtures also include production inventory and copy rejection for hostile links, FIFOs, sockets, and hard links, retained-ancestor write substitution, temporal checker/shim substitutions, after-mutation waiter rejection and EOF restoration, retry-safe restoration, TERM/KILL, and terminal cleanup without story, simulator, remote work, or retained residue.

Inside the unchanged outer `deny network*` sandbox, the first byte-identical installed-script invocation supplies byte-equal physical `RN_ROOT` and `REACT_NATIVE_PATH`; its real inner `env -i` forwards the resolved `RN_ROOT`, `PODS_ROOT`, `HOME`, selected-Xcode `PATH`, and `DEVELOPER_DIR` to one exact-argv shim. The selected developer directory is physical, and resolved `swift`, `swiftc`, `xcodebuild`, `lipo`, `dsymutil`, `otool`, `dwarfdump`, and simulator SDK records must be component-contained and exact in path/type/mode/device/inode/size. Exact requested/physical identity records are separately retained for `/usr/bin/install_name_tool`, `/usr/bin/sandbox-exec`, `/usr/bin/env`, `/usr/bin/python3`, `/usr/bin/xcrun`, and `/usr/bin/xcode-select`. The shim re-resolves and physically compares the exact selected tool immediately before every `swift`, `lipo`, `otool`, `dsymutil`, or `dwarfdump` invocation; the verified physical Swift target is executed with literal `swift` as argv[0] so a realpath-resolved `swift-frontend` retains Swift driver dispatch. Immediately before each architecture's `swift build`, its SDK accessor also runs `xcrun --sdk iphonesimulator --show-sdk-path` under the explicit selected `DEVELOPER_DIR`, rechecks containment and the complete recorded SDK identity, and returns only that checked path. The shared validator does the same before every selected-tool use, and each main-app `xcodebuild` use is separately guarded. The focused regression substitutes the recorded SDK directory with a same-mode directory between selection and use, proves the identity accessor rejects it, then restores the original directory. The shim performs ordered `arm64` then `x86_64` native builds, validates thin and universal simulator platform/minimum/install-name facts and dSYM UUIDs, then blocks while a bounded promotion worker validates exact public module/header outputs and writes one canonical unsigned simulator framework `Info.plist` through the held framework descriptor. Checker and shim mode, size, digest, and inode identity are retained at pre/mid/post temporal points. Executable same-byte substitutions after checker/shim launch but before checker execution or promotion reject use with independently unchanged package streams.

Before the main app build, one shared final validator runs after both the first installed-script build and identical second cache-hit invocation. It requires the exact three-key XCFramework plist and all six complete reviewed slice dictionaries, exactly the same six root slice directories and no extra disk slice, and carries then byte-compares both exact canonical B1 stream bytes and SHA-256 for all five non-target slices rather than a digest-only summary. Both snapshots come from the bounded inventory worker through the held package descriptor, component validation, no-follow opens, held-chain rechecks, stat/fstat identity checks, and absolute/escaping-link rejection; no path-based Node walker is used. It also requires the exact simulator target framework/binary/dSYM/hash, two simulator architectures at iOS 16.4, rewritten install names, UUID equality, ten public module artifacts, reviewed header/modulemap/C++ content, exactly one canonical simulator framework plist, and no Project, private, or package interfaces. The focused regression executes the same validator against one positive contract and independent one-field negatives for target and non-target plist dictionaries, missing/extra plist and disk slices, wrong framework plist, non-target B1 bytes, Mach-O, dSYM, module/header/modulemap, forbidden-entry, and build-hash failures. The direct second invocation requires `[cache-hit, shim-marker, simulator-build]` exactly `[1,0,0]`; the successful main `xcodebuild -quiet` path allows its normal cache line to be suppressed, so it requires `[0|1,0,0]`. A cache miss cannot pass silently because the shim requires the descriptor-promotion stdin handshake that only the first supervised invocation supplies. The focused regression rejects duplicate cache hits, shim markers, and concealed simulator builds independently for both paths. No process may address or connect to a product production server; no credentials, product endpoint, package/dependency change, G005 surface, live service, or CDN operation is authorized.

The compatibility patch was introduced while the selected Xcode 26.3 was below [Expo SDK 57's supported Xcode 26.4 minimum](https://docs.expo.dev/versions/latest/), and stable `expo-modules-core` 57.0.3-57.0.7 still shipped the EventEmitter capture rejected by Swift 6.2.4. Attempt 12 explicitly selects `/Applications/Xcode.app/Contents/Developer`, observed as Xcode 26.6 with Swift 6.3.3, instead of mutating the machine-wide `xcode-select` setting. `scripts/patch-expo-modules-core.mjs` continues to carry only the EventEmitter half of Expo's [upstream fix](https://github.com/expo/expo/commit/d01580058a40e2650efca4dad1015255d500bf99): the installed 57.0.3 package already contains the byte-identical `NonisolatedUnsafeWeakVar` half. The script pins the exact package version, legacy/fixed EventEmitter hashes, and helper hash; applies once or accepts the fixed bytes; and rejects every drift. `postinstall`, `preios`, and `preverify:ui` enforce it, while the unit regression applies then checks it idempotently so `npm ci --ignore-scripts` remains usable. This does not downgrade Swift, weaken concurrency checking, select a canary, enable remote precompiled modules, or change production behavior. Remove the hooks and patch after adopting a stable core whose EventEmitter bytes are already fixed, then reinstall dependencies.

Story attempts 8, 9, and 10 are consumed. After G012 repaired generic failure transport, the user explicitly authorized exactly one additional G013 attempt 11. Attempt 11 ran the canonical G004 story once: the first six phases passed, then Development Build emitted one valid redacted `ERR_ASSERTION` generic envelope with zero cleanup errors and failed. Log `.omx/handoff/g004-story-acceptance-attempt-11.log` has SHA-256 `b004c6be5f68b8f74103439ef50ee0565f1f9ac6d65e68fb0d711373b535f326`. Attempt 11 is consumed without retry, alternate story/UI command, retained cache, or G005/G010 progression.

On 2026-07-23 the user authorized exactly one additional attempt 12 after the pinned Gradle bootstrap and the complete stated pre-Story sequence pass. This supersedes the attempt-11 no-retry boundary only for that one canonical `npm run verify:story -- G004-build-generic-xmf-ui-path` invocation. A focused or preflight failure does not consume attempt 12 and must be repaired before Story; once the Story starts, success or failure consumes it and no alternate UI/Story command or automatic rerun is authorized.

Attempt 12 ran that canonical Story command exactly once on 2026-07-24 after the full pre-Story sequence passed. `contract-registry` and all five targeted phases passed; the Development Build reached its result assignment without a build/runtime primary, then its cleanup-only path emitted one canonical `UNCLASSIFIED` `AggregateError` envelope with phase `cleanup`, `cleanupErrorCount:1`, and evidence SHA-256 `427d44026e147054b66b4628946709a5e73cad1ee697c82d493af24fce339a96`. The Story exited 1 after approximately 215 seconds, so attempt 12 is consumed and G004 is not complete. The immediate local audit found no repository `ios/`, `android/`, or `.omx/tmp`, no G004 temp root, no owned process or Metro listener, no installed `com.anonymous.allnewmts` simulator app, and no booted simulator. The value-redacted cleanup envelope does not identify which bounded cleanup action raised the recorded error, so no causal repair or retry may be claimed from this evidence alone.

The active attempt-12 cleanup repair is bounded to that cleanup-only failure and authorizes no Development Build, full `verify:ui`, Story, milestone, remote operation, G005/G006, packaging, or product endpoint/CDN access before the user's next explicit attempt approval. A later read-only correlation of the retained CoreSimulator and MobileInstallation logs identified the missing durability boundary: the first uninstall destroyed the bundle and data containers and queued a LaunchServices unregister operation, but the runner accepted the immediately absent `get_app_container` result and shut the simulator down before that operation completed. After reboot, the stale registration triggered a no-container uninstall and a canceled unregister operation. Container absence therefore was not registration absence. The repair exact-parses `simctl listapps` through `/usr/bin/plutil`, requires the dual-signal stable state described above before shutdown and again after reboot, and bounds simulator state transitions. `node scripts/run-g004-development-build.mjs --simulator-cleanup-regression` exercises only an injected command/state fixture: it reproduces container-first disappearance, delayed LaunchServices removal, post-reboot stale registration, a nonzero idempotent uninstall, persistent-stale failure, command/plist failure, exact bundle-key matching, and bounded shutdown transitions without invoking Xcode, `simctl`, a simulator, Metro, an app install, the network, or repository mutation. Focused static/unit checks and `verify:fast` may validate this repair; only a later explicit user approval can authorize the next canonical Story attempt.

Cleanup-repair implementer record (2026-07-24): the bounded changed paths remain `docs/testing.md`, `scripts/run-g004-development-build.mjs`, `test/foundation.test.mjs`, and `verification/manifest.json`; this repair adds no generated, frozen, oracle, package-lock, dependency-cache, or product bytes. Its selected risk is native-simulator cleanup state, with injected regression, focused phase/preflight diagnostics, unit, and fast tiers; a Development Build, unphased/full UI, Story, milestone, remote operation, Android integration execution, G005/G006, packaging, and product endpoint/CDN work are explicit non-goals. The private simulator-cleanup regression passed in 0.04 s and proved the closed fixture cases above. The pre-attempt diagnostics passed asset `--check` in 0.03 s, docs in 0.24 s, policy in 0.46 s, type in 0.80 s, `parser-model` in 0.52 s, `projection-render` in 0.51 s, `runtime-client` in 0.49 s, and `unseen-generality` in 0.50 s. The first `module-stub-smoke` diagnostic exited 1 because that shell invocation omitted the already-selected Xcode `DEVELOPER_DIR`; it made no repository or simulator mutation and consumed no Story attempt. Repeating only that failed focused diagnostic with `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer` passed in 8.12 s, then Development-Build `--preflight` passed in 0.86 s with `mutatedFiles:false`, unit passed in 68.48 s, and fast passed all five checks in 70.60 s. Generated/frozen/oracle diffs are `none`; residue checks found no root `ios/` or `android/`, G004/G011 temp entry, `.omx/tmp`, or booted simulator. The remaining risk is that the injected regression cannot itself reproduce live LaunchServices scheduling; only a separately authorized canonical Story can prove the repaired end-to-end cleanup, so G004 remains incomplete. Rollback removes only the cleanup helper/regression/mode, its production call-site and test/contract hunks, and their integrity-hash updates while retaining the earlier attempt-12 dependency/G011 repairs; no local cache rollback applies because this repair installed or changed none. A separate non-implementing `APPROVE|REQUEST CHANGES` and `CLEAR|NOT CLEAR` report remains outstanding; this implementer claims neither.

On 2026-07-24 the user explicitly authorized exactly one additional attempt 13 after the cleanup-repair regression, focused diagnostics, preflight, unit, and fast checks passed. This authorizes only one `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer npm run verify:story -- G004-build-generic-xmf-ui-path` invocation. Success or failure consumes attempt 13; no alternate UI/Story command, automatic rerun, milestone, G005/G006 progression, packaging, remote operation, or product endpoint/CDN access is authorized by this approval.

Attempt 13 ran that canonical command exactly once and passed. The single UI check exited 0 in 199.968 s with exactly one UI and one Development-Build invocation; `contract-registry`, all five targeted phases, `development-build`, and `policy-cleanup` passed in their required order. The Development Build passed in 193.040 s with the exact readiness source hash `4d63ba22ac5339cfd3068cffa91710e0099481da81d974e2aff0ce7ae39ed53e`, one form, two labels, one edit, two buttons, module `AllNewMTSRuntime`, create code `OK`, one guarded-port selection, local-cache Apple dependencies, and no cleanup aggregate. The repaired cleanup completed its pre-shutdown and post-reboot dual-signal absence proofs, and the Story aggregator returned `PASS` for `G004-build-generic-xmf-ui-path`; attempt 13 is consumed successfully and was not rerun. The approval-bound pre-Story fast run had also passed all five checks in 69.40 s. The immediate residue audit found no root `ios/` or `android/`, `.omx/tmp`, G004/G011 temp root, selected-port listener, owned runner/Metro/log process, or booted simulator, while the tracked paths remained exactly `docs/testing.md`, `scripts/run-g004-development-build.mjs`, `test/foundation.test.mjs`, and `verification/manifest.json`; generated/frozen/oracle diffs remain `none`. This Story PASS does not activate or prove milestone, packaging, Android integration execution, G005/G006, live service, remote, or product CDN work. A separate non-implementing `APPROVE|REQUEST CHANGES` and `CLEAR|NOT CLEAR` report remains outstanding, so this implementer records the successful canonical acceptance but claims neither review decision.

Attempt-12 implementer record: scope was exact local dependency provisioning, the G011 absent-`.omx/tmp` oracle repair needed to restore fast verification, the complete G004 pre-Story sequence, and the single authorized Story run; Android integration execution, G005/G006, product endpoints/CDN, packaging, and a second Story/UI run were non-goals. The selected tiers were focused diagnostics, fast, and the single 20-minute-budget native Story. The final pre-Story sequence passed in order: asset `--check` 0.03 s, docs 0.23 s, policy 0.47 s, type 0.80 s, `parser-model` 0.53 s, `projection-render` 0.50 s, `runtime-client` 0.54 s, `unseen-generality` 0.49 s, `module-stub-smoke` 6.91 s, Development-Build `--preflight` 0.85 s, unit 65.22 s, and fast 73.59 s, all exit 0. The sole `npm run verify:story -- G004-build-generic-xmf-ui-path` then exited 1 as recorded above; every post-Story fast run passed all five checks, with the final recorded unit check taking 73.18 s. Generated/frozen/oracle byte diffs are `none`; the deterministic tracked diff is limited to `docs/testing.md`, `scripts/run-g004-development-build.mjs`, `test/foundation.test.mjs`, and `verification/manifest.json`, including their manifest hashes and the unit budget increase from 60 to 120 seconds. At that point the remaining risk was the unidentified cleanup action, so its rollback meant reversing those four bounded tracked-file hunks together; the later cleanup-repair record above supersedes only that risk and repair rollback. Local cache rollback may remove only the exact recorded Gradle key, Android package/version directories, command-line-tools directory, and two new CocoaPods cache keys after identity revalidation and confirmation that no other project uses them. No rollback was performed because fast and all focused/preflight checks pass. A separate non-implementing `APPROVE|REQUEST CHANGES` and `CLEAR|NOT CLEAR` report remains outstanding; this implementer claims neither.

G012 changes only failure transport and authorizes no G004, full UI, or story execution. Every Development-Build primary, including cleanup-only `AggregateError`, now uses the same synchronous marker writer. Only runner-created compiled-build errors retain the frozen Xcode evidence/hash; a private identity set distinguishes them, so colliding ordinary-error properties cannot bypass generic formatting. Ordinary-error envelopes contain a fixed redacted `allnewmts.g004.generic-failure-evidence.v1` object capped at 1,024 canonical bytes; messages, stacks, arbitrary names/codes, and preseeded evidence are excluded, with only `ERR_ASSERTION` or `UNCLASSIFIED` classifications. The parent independently requires that schema's exact five keys, fixed value domains, hash, canonical bytes, and cap; unknown, extended, invalid, or oversized generic evidence becomes the value-free fallback. One primary object can emit at most once and remains the thrown object. Cleanup-only aggregates own the same ordered `errors`/`cleanupErrors` array passed to the writer, so a writer error is appended once without replacing the primary. The private forwarding regression proves ordinary assertion transport, hostile code/property redaction, schema/cap rejection, duplicate prevention, aggregate writer-failure ordering, and byte-identical parent forwarding without build, simulator, Metro, network, or repository mutation.

G014 keeps that exact five-key generic evidence and closes `phase` to `development-build`, `package-custodian`, `environment-selection`, `offline-dependencies`, `prebuild`, `pods`, `nested-swiftpm`, `build-settings`, `compiled-build`, `simulator-boot`, `metro`, `app-install`, `app-launch`, `runtime-marker`, or `cleanup`. The runner snapshots its owned phase with the primary before cleanup; cleanup-only failure uses `cleanup`, while an unknown producer value falls back to `development-build` and an unknown parent value is rejected to the value-free forwarding fallback. Preflight and the immediately following Git baseline capture remain before phase state and the cleanup-owning production boundary. Compiled Xcode evidence remains byte-identical because its private evidence path ignores generic phase formatting. Independent producer and parent lists are compared and every production phase is canonically forwarded by the private transport regression; no G004, simulator, Metro, UI/story, live service, or CDN execution is authorized.

The G004 rollback artifact content-pins every final-G003 shared file, including `modules/allnewmts-lua/src/runtime.ts`, with exact bytes, byte count, SHA-256, and per-hash provenance from pre-edit session artifacts and the final-G003 integrity inventory. The policy/cleanup phase reconstructs only those shared paths in a temporary selective rollback, returns their reproduced hashes, proves UI/G004 deferment is restored, and leaves G003-only paths outside rollback.

The focused verifier pins its generator, UI verifier, Development-Build runner, runtime golden, and recorded G003 path baseline in the integrity inventory. `policy-cleanup` requires every current dirty path to remain inside the union of that baseline and the dry G004 rollback inventory, checks their declared shared-path intersection, and byte-compares package-lock and immutable-oracle provenance to the recorded checkpoint. Committed paths need not remain dirty. This is a path/shared-owner rollback boundary, not a second G003 acceptance or a claim that shared-file G003 bytes were reverted.

The exact G004 editing sequence is generated-asset `--check`, docs, policy, type, the five phase commands in the order above, Development Build `--preflight`, unit, then fast. `npm run verify:ui` is not separately invoked: exactly one final `npm run verify:story -- G004-build-generic-xmf-ui-path` owns its single UI and Development Build invocations. Fast, phase, stub, and preflight output are diagnostics only.

## Change protocol

Every change must record:

1. goal and governing spec links;
2. bounded changed paths and explicit non-goals;
3. risk classification, selected tier, and why;
4. the single acceptance command, each invoked focused check, duration, exit code, and result;
5. deterministic fixture/generated/hash diffs or an explicit `none`;
6. remaining risks plus cleanup and rollback instructions; and
7. a separate non-implementing `APPROVE|REQUEST CHANGES` and `CLEAR|NOT CLEAR` report.

Update governing Markdown before or atomically with affected code and manifests. Implementers cannot approve their own changes. `verify:fast` alone cannot close a story or milestone. Structural copying and call-graph reproduction are independent review judgments; similarity heuristics never decide acceptance.

## Failure protocol

An activated check cannot silently skip or succeed as an empty placeholder. An unactivated layer reports `DEFERRED(<owning-goal>)`. Failures name the violated contract and the smallest focused rerun. Clean temporary outputs and restore deterministic inputs before review.
